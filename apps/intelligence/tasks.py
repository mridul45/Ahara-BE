# Celery tasks — reserved for future use when a background worker is available.
# Render free tier does not support background processes; consolidation is
# triggered manually via: python manage.py memory_consolidate
